{
  "name": "ecommerce",
  "version": "0.1.0",
  "private": true,
  "description": "Storefront demo — Angular frontend, Node/Express/MongoDB API, and a data-cleaning pipeline.",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "data": "node data/clean.js",
    "seed": "npm --prefix backend run seed",
    "start:backend": "npm --prefix backend start",
    "start:frontend": "npm --prefix frontend start"
  },
  "engines": {
    "node": ">=18"
  }
}
